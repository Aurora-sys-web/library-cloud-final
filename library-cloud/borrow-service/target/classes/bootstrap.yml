spring:
  application:
    name: borrow-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: properties
        group: DEFAULT_GROUP
        refresh-enabled: true
  # 导入 Nacos 配置
  config:
    import: optional:nacos:borrow-service.properties