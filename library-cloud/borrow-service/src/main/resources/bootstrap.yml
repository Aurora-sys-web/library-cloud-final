spring:
  application:
    name: borrow-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: properties
        group: DEFAULT_GROUP
        refresh-enabled: true
